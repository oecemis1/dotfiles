#!/usr/bin/env bash
if hyprctl clients -j | jq -e '.[] | select(.title == "my_todo")' > /dev/null; then
    hyprctl dispatch togglespecialworkspace todo
else
    kitty --title my_todo -e todo.sh &
fi
