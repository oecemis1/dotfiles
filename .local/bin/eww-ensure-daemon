#!/usr/bin/env bash
exec 9>"${XDG_RUNTIME_DIR:-/tmp}/eww-daemon-spawn.lock"
flock 9

eww ping >/dev/null 2>&1 && exit 0

eww daemon
for _ in $(seq 30); do
    eww ping >/dev/null 2>&1 && exit 0
    sleep 0.1
done
exit 1
